package hbase;

import org.apache.hadoop.hbase.util.Merge;

public class HBaseRepaireUtil {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		Merge merge = new Merge(Conf.getConfiguration());
		merge.
	}

}
